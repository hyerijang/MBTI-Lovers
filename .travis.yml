language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "sudo ./gradlew build -x test"

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - hyeri3316@g.hongik.ac.kr

before_install:
  - sudo chmod 777 ./gradlew 